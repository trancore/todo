import { Meta } from '@storybook/react';
import TodoModal from '~/components/presentational/Modal/TodoModal';

const meta: Meta<typeof TodoModal> = {
  title: 'Modal/TodoModal',
  component: TodoModal,
  tags: ['autodocs'],
  // Add your own control here
};
export default meta;
